#include "Pila.h"
#include <iostream>
using namespace std;
Pila::Pila()
{
    cima = -1;
}

bool Pila::Apilar(TipoDato& elemento)
{
    bool res;
    if (cima == MAX - 1)
    {
        cout << "Desbordamiento de Pila (Overflow)" << endl;
        res = false;
    }
    else
    {
        cima++;
        pila[cima] = elemento;
        res = true;
    }
    return res;
}

bool Pila::Desapilar()
{
    bool res;
    if (cima == -1)
    {
        cerr << "Se esta intentando quitar un elemento de una pila vacia (underflow)" << endl;
        res = false;
    }
    else

    {
        cima--;
        res = true;
    }
    return res;


}

void Pila::VerPila()
{
    for (int i = 0;i <= cima;i++)
        cout << pila[i] << endl;
}


bool Pila::CimaPila(TipoDato& elemento)
{
    bool res;
    if (PilaVacia())//(cima == -1)
    {
        cerr << "Se esta intentando quitar un elemento de una pila vacia (underflow)" << endl;
        res = false;
    }
    else
    {
       elemento= pila[cima];
       // cima--;
        res = true;
    }
    return res;
}

bool Pila::PilaVacia()
{
    return cima == -1;
}

void Pila::LimpiarPila()
{
    cima = -1;
}


bool Pila::Iguales(Pila pi, Pila p2)
{
// pilasestaticas.cpp : Este archivo contiene la función "main". La ejecución del programa comienza y termina ahí.
//
#include <iostream>
#include "Pila.h"
#include <stdlib.h>
using namespace std;
int main()
{
	int opcion,r,r1;
	Pila pi, p2;//Llama al constructor
	do {
		cout << "Bienvenido al programa de Funciones de la Pila:"<<endl;
		cout << "1.Apilar pila(PUSH)"<<endl;
		cout << "2.Desapilar (pop):"<<endl;
		cout << "3. Ver elemento de la pila:"<<endl;
		cout << "4.Limpiar pila:"<<endl;
		cout << "5.Iguales:" << endl;
		cout << "6.Ver pila:"<<endl;	
		cout << "7.Limpiar pila:"<<endl;
		cin >> opcion;
		switch (opcion) {
		case 1:
			cout << "Ingresa el dato a apilar(pila 1):";
				cin >> r;
				if (pi.Apilar(r)) {
					cout << "se procesa el dato;";
				}
			cout << "Ingresa el dato a apilar(pila 2):";
			cin >> r1;
				if (p2.Apilar(r1)) {
					cout << "se procesa el dato;";
				}

			break;
		case 2:
			if (pi.Desapilar()) {
				cout << "Se procedio a desapilar; ";
			}
			break;
		case 3:
			if (pi.CimaPila(r))
			cout << "El elemento en la pila es:"<<r<<endl;
			break;
		case 4:
			if (pi.PilaVacia())
				cout << "La pila esta vacia:" << endl;
			else
				cout << "La pila esta llena" << endl;
			break;
		case 5:
			break;
		case 6:
			pi.VerPila();
			break;
		case 7:
			pi.LimpiarPila();
			break;
		}
	} while (opcion !=0);
} #pragma once
//typedef int TipoDato;
#define MAX 100
using TipoDato = int;
/*************************************************************/
//                      pila.h
/*************************************************************/
class Pila
{
private:
    int pila[MAX];
    int cima;

public:
    Pila();
    bool Apilar(TipoDato& elemento);
    bool Desapilar();
    bool CimaPila(TipoDato& elemento);
    void LimpiarPila();
    void VerPila();
    bool PilaVacia();
    bool Iguales(Pila p,Pila p2);

};

  if (cima)
}
